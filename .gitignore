/target
**/*.rs.bk
Cargo.lock
*.pdb
.vscode/
.idea/
*.log
*.jsonl
/tmp/
/.omc/
/.claude/
crates/gui-host/assets/*.png
target-docker/
tests/fixtures/libnppicc-stub.so

# Wrangler local emulator state — only present when wrangler is run
# inside the repo for R2 mirror maintenance (see packaging/windows/README.md).
.wrangler/

# prdt host identity key — generated by `prdt host --key-file host-key.bin`
# during local smoke runs. 32-byte Noise private key: MUST never be committed.
host-key.bin
*.key.bin

# scripts/fetch-ci-artifacts.{ps1,sh} + scripts/e2e-smoke.{ps1,sh} work dir:
# downloaded CI binaries, per-run Noise identity keys, and smoke-test logs.
/.smoke-artifacts/
